version: "3.0"

services:
  server:
    image: ghcr.io/memory-io/memory_server:latest
    build: ./packages/memory_server
    ports:
      - "6969:8000"
    environment:
      - MONGO_URI=mongodb+srv://connerlreplogle:poH4LtWfmRQQsXf9@cluster0.mf9snea.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0
      - DATABASE_NAME=memory
    links:
      - "db:database"
    depends_on:
      - db
  website:
    image: ghcr.io/memory-io/memory_web:latest
    build: ./packages/memory_web
    ports:
      - "3000:3000"
    environment:
      - API_URL=http://localhost:6969
      - ORIGIN=https://m3m0ry.io
    links:
      - "server:server"
    depends_on:
      - server
    volumes:
      - server-data:/app/data
volumes:
  server-data: {}

    